{
  "name": "homebridge-notify-alerts",
  "version": "1.3.0",
  "displayName": "Notify Alerts",
  "description": "A Homebridge plugin that sends push notifications via the Notify! API using HomeKit switches",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && tsc",
    "prepublishOnly": "npm run build",
    "watch": "npm run build && npm link && nodemon"
  },
  "keywords": [
    "homebridge-plugin",
    "homebridge",
    "notify",
    "webhook",
    "notification"
  ],
  "author": "simplytoast1",
  "license": "Apache-2.0",
  "files": [
    "dist",
    "config.schema.json",
    "homebridge-ui"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/simplytoast1/homebridge-notify-alerts.git"
  },
  "bugs": {
    "url": "https://github.com/simplytoast1/homebridge-notify-alerts/issues"
  },
  "homepage": "https://getnotifyapp.com",
  "engines": {
    "homebridge": "^1.8.0 || ^2.0.0",
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@homebridge/plugin-ui-utils": "^1.0.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "homebridge": "^1.7.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.3.0"
  },
  "nodemonConfig": {
    "watch": [
      "src"
    ],
    "ext": "ts",
    "ignore": [],
    "exec": "tsc && homebridge -D -I -U ./"
  }
}
